node_modules/
.env
dist/
coverage/

# This project publishes its agent tooling. Everything else under .claude/ is
# private, and local working state lives in .local/, both covered globally.
!.claude/agents/
!.claude/commands/
!.claude/skills/
!.claude/settings.json
