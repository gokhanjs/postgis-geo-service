See [AGENTS.md](AGENTS.md).

That file is the single source of the architectural invariants, conventions and
verification steps for this repository. It is kept vendor-neutral so every tool
reads the same rules.
